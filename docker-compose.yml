version: '3.6'
services:
  web:
    image: 'gitlab/gitlab-ce:latest'
    restart: always
    hostname: '192.168.0.12'
    container_name: gitlab
    environment:
      GITLAB_OMNIBUS_CONFIG: |
        external_url 'https://192.168.0.12:7743'
        gitlab_rails['gitlab_shell_ssh_port'] = 7722
        nginx['enable'] = true
        # nginx['listen_port'] = 80 # http
        nginx['redirect_http_to_https'] = true # https
        nginx['ssl_certificate'] = "/home/git/data/gitlab/ssl/gitlab.crt" # https cert
        nginx['ssl_certificate_key'] = "/home/git/data/gitlab/ssl/gitlab.key" # https key
        nginx['client_max_body_size'] = '500G'
      GITLAB_TIMEZONE: Asia/Seoul
    ports:
      # http
      - '7780:80'
      # https
      - '7743:443'
      # ssh
      - '7722:22'
    volumes:
      - '/home/git/data/gitlab/config:/etc/gitlab'
      - '/home/git/data/gitlab/logs:/var/log/gitlab'
      - '/home/git/data/gitlab/data:/var/opt/gitlab'
      - '/home/git/data/gitlab/backups:/var/opt/gitlab/backups'
      - '/home/git/data/gitlab/ssl:/etc/gitlab/ssl:ro' # https set
    # container size (default: 256m)
    shm_size: '8G'